# Local: talk to the API directly. Leave unset in Railway so the UI uses /api
# and the Next.js proxy (API_INTERNAL_URL).
NEXT_PUBLIC_API_URL=http://localhost:8000

# Railway / production only. Private or public URL of the backend service.
# API_INTERNAL_URL=http://resumeagent-backend.railway.internal:8000
