# Local: talk to the API directly. Leave unset in Railway so the UI uses /api
# and the Next.js proxy (API_INTERNAL_URL).
NEXT_PUBLIC_API_URL=http://localhost:8000

# Railway / production only. Must include http:// or https://
# API_INTERNAL_URL=https://resumeagent-backend-production.up.railway.app
